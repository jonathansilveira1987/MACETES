num = input('Número > ')

def find_max (nums):
    max_num = float ( "-inf" ) # menor que todos os outros números 
    num == (nums)
    if num > max_num:
# (Preencha a linha que falta aqui)
        return max_num

def find_max (nums):
    max_num = float ( "-inf" ) # menor que todos os outros números 
    for num in nums:
        if num > max_num:
    # (Preencha a linha que falta aqui)
            return max_num